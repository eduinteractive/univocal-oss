# syntax=docker/dockerfile:1.7
# -------- Builder --------
FROM node:20-alpine AS builder
WORKDIR /service
RUN npm i -g pnpm

# NUR im Builder verwenden (kommt nicht ins Runtime-Image)
ARG NPM_TOKEN
RUN echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > .npmrc
RUN pnpm config set inject-workspace-packages true --location project

# Diese COPYs erwarten, dass der Build-Kontext das Repo-Root ist (Skaffold: context: .)
COPY pnpm-workspace.yaml pnpm-lock.yaml package.json ./
COPY lib ./lib

RUN pnpm install --filter ./lib/*
RUN pnpm run build:libs

COPY core/uvc-survey/src ./core/uvc-survey/src
COPY core/uvc-survey/migrations ./core/uvc-survey/migrations
COPY core/uvc-survey/migrate-mongo-config.js ./core/uvc-survey/migrate-mongo-config.js
COPY core/uvc-survey/tsconfig.prod.json ./core/uvc-survey/tsconfig.prod.json
COPY core/uvc-survey/package.json ./core/uvc-survey/package.json

# Nur das Zielpaket + transitive Workspace-Deps installieren
RUN --mount=type=cache,target=/root/.pnpm-store \
    pnpm install --frozen-lockfile --filter ./core/uvc-survey...

# Build nur für das Zielpaket
RUN pnpm --filter ./core/uvc-survey run build:prod

# Deployment-Paket mit NUR Prod-Deps erzeugen
RUN pnpm --filter ./core/uvc-survey deploy --prod /opt/app

# -------- Runtime --------
FROM node:20-alpine AS runtime
ENV NODE_ENV=production
WORKDIR /app

# Nur das minimal nötige Artefakt kopieren
COPY --from=builder /opt/app ./
COPY --from=builder /service/core/uvc-survey/migrations ./migrations

ENV npm_package_name=uvc-survey

EXPOSE 3007
# Passe an dein start-Script an (empfohlen "start" statt "dev")
CMD ["node", "build/src/service.js"]
                
    